const config = {
  routes: {
    // Home
    home: "/",

    // Auth
    login: "/login",
    register: "/register",

    // User
    profile: "/profile",
    messages: "/messages",
    notifications: "/notifications",

    // Not found
    notFound: "*",
  }
}

export default config;